const express = require('express'); const { requireAuth } = require('../middleware/auth'); const globalSearch = require('../services/global-search'); const workspace = require('../services/user-workspace'); const router = express.Router();
router.get('/', requireAuth, async (req, res, next) => { try { const data=await globalSearch.search(req.query.q, req.session.user.role); await workspace.recordSearch(req.session.user.id,data.query,data.total); const pinned=await workspace.pins(req.session.user); res.render('search/index', { title: 'অনুসন্ধান', data, pinnedHrefs:new Set(pinned.map(x=>x.href)) }); } catch (e) { next(e); } }); module.exports = router;
